import React from 'react';
import { Switch } from 'react-router-dom';


const AdminLoginPage = () => {
  // Giao diện đăng nhập cho admin
  return (
    <div>
      <h1>Đăng nhập cho admin</h1>
      {/* Thêm form đăng nhập cho admin tại đây */}
    </div>
  );
};

export default AdminLoginPage;
